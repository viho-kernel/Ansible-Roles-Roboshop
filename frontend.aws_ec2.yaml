plugin: amazon.aws.aws_ec2
regions:
- us-east-1

filters:
  instance-state-name: running
  tag:Project: roboshop
  tag:Environment: "{{ enviro }}"
  
keyed_groups:
  - key: tags.component
    seperator: ' '

compose:
  ansible_host: private-ip-address
  
hostnames:
- private-ip-address